<!DOCTYPE html>
<html class="h-full bg-gray-50">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="h-full">
        {% block navigation %}
        <header class="absolute inset-x-0 top-0 z-50">
            <nav class="flex items-center justify-between p-6 lg:px-8" aria-label="Global">
            <div class="flex lg:flex-1">
                <a href="{{ path('landing') }}" class="-m-1.5 p-1.5">
                    <img class="h-8 w-auto" src="{{ asset('img/turtunicorn.png') }}" alt="">
                </a>
            </div>
            <div class="flex gap-x-12">
                <a href="#" class="text-sm font-semibold leading-6 text-gray-900">What's this?</a>
                <a href="#" class="text-sm font-semibold leading-6 text-gray-900">How does it work?</a>
                <a href="#" class="text-sm font-semibold leading-6 text-gray-900">BUY IT ALREADY!</a>
            </div>
            <div class="lg:flex lg:flex-1 lg:justify-end">
                {% if is_granted('IS_AUTHENTICATED') %}
                    <a href="{{ url('add_link') }}" class="text-sm font-semibold leading-6 text-gray-900">
                        My account <span aria-hidden="true">&rarr;</span>
                    </a>
                {% else %}
                    <a href="{{ path('login') }}" class="text-sm font-semibold leading-6 text-gray-900">
                        Sign in <span aria-hidden="true">&rarr;</span>
                    </a>
                {% endif %}
            </div>
            </nav>
        </header>
        {% endblock %}
    
        {% block body %}{% endblock %}
    </body>
</html>
